# ═══════════════════════════════════════════════════════════════════
# Behavioral Agentic AI — Production Dependencies
# Pinned versions for reproducible builds
# Last updated: 2026-02-15
# Python: 3.13+
# ═══════════════════════════════════════════════════════════════════

# ── Core Framework ───────────────────────────────────────────────
fastapi==0.125.0
uvicorn[standard]==0.38.0
python-multipart==0.0.21
starlette==0.50.0

# ── Database ─────────────────────────────────────────────────────
SQLAlchemy==2.0.45
aiosqlite==0.22.0
greenlet==3.3.0

# ── Data Validation ──────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.12.0
pydantic_core==2.41.5
email-validator==2.3.0
annotated-types==0.7.0

# ── Security & Authentication ────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
cryptography==46.0.3

# ── NLP — Sentiment Analysis (HuggingFace BERT Primary) ─────────
transformers==4.57.3
torch==2.9.1
tokenizers==0.22.1
safetensors==0.7.0
huggingface-hub==0.36.0

# ── NLP — Fallback Analyzers ────────────────────────────────────
vaderSentiment==3.3.2
textblob==0.19.0
nltk==3.9.2

# ── NLP — Language Detection ────────────────────────────────────
langdetect==1.0.9

# ── RAG — Embeddings ────────────────────────────────────────────
sentence-transformers==5.2.2
scikit-learn==1.7.2
scipy==1.16.3
numpy==2.3.1

# ── RAG — Vector Store ──────────────────────────────────────────
chromadb==1.4.1
onnxruntime==1.24.1
mmh3==5.2.0

# ── LLM Integration ─────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9

# ── Google Generative AI (optional, Gemini API) ─────────────────
google-generativeai==0.8.6
google-genai==1.62.0
google-api-core==2.25.1
google-auth==2.48.0

# ── Document Processing ─────────────────────────────────────────
PyPDF2==3.0.1
python-docx==1.2.0
lxml==6.0.2

# ── Environment & Configuration ──────────────────────────────────
python-dotenv==1.2.1
PyYAML==6.0.3

# ── Date/Time ────────────────────────────────────────────────────
python-dateutil==2.9.0.post0

# ── WebSockets (real-time escalation alerts) ─────────────────────
websockets==15.0.1

# ── Production Utilities ─────────────────────────────────────────
orjson==3.11.7
tqdm==4.67.1
regex==2025.11.3
joblib==1.5.2
filelock==3.20.1

# ── Networking ───────────────────────────────────────────────────
requests==2.32.4
certifi==2025.6.15
urllib3==2.5.0
idna==3.10
anyio==4.12.0
sniffio==1.3.1
h11==0.16.0

# ── Testing (dev only — can exclude in production) ───────────────
pytest==9.0.2
pytest-asyncio==1.3.0
